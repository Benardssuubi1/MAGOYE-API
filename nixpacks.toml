
[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install gunicorn flask flask-cors psycopg2-binary"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
